apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hugo-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: hugo-tls
  issuerRef:
    name: {{ .Values.tls.issuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.domain }}
