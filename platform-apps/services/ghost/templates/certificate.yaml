apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ghost-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: ghost-tls
  issuerRef:
    name: {{ .Values.tls.issuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.domain }}
    - www.{{ .Values.domain }}
